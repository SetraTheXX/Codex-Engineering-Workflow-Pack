# Domain Docs

Domain language document:

```txt
CONTEXT.md
```

ADR directory:

```txt
docs/adr/
```

Current project domain:

```txt
Codex Engineering Workflow Pack: a local-first, composable skill pack for disciplined Codex software engineering workflows.
```

Workflow skills should read `CONTEXT.md` before using project-specific terms. ADRs should be used only for durable architectural or product workflow decisions that would be surprising without context.

If domain terms are unclear, use `grill-with-docs` before creating PRDs, issues, architecture reports, or implementation plans.
